let newVariable = 'Playing around with typeof.';

console.log(typeof newVariable);

newVariable = 1

console.log(typeof newVariable);

console.log(typeof newVariable);

/* On lines 3, 7, & 9 I used the "typeof" operator to print the type of data within the "newVariable" variable to the console. Line 3 printed "string" to the console because that was the type of data within that variable. Lines 7 and 9 printed "number" to the console because "newVariable" was reassigned the number 1 on line 5. */