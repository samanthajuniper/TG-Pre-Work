const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('sweep floor', 'grocery shop');
console.log(chores);

/* The ".push()" function allows you to add an element to the end of an array. On line 3 I added the ".push()" function to the chores" array in order to add "sweep floor" & "grocery shop" to the end of the initial array. */