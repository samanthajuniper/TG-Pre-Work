const chores = ['wash dishes', 'do laundry', 'take out trash', 'cook dinner', 'mop floor'];

chores.pop();
console.log(chores);


/*The ".pop()" function removes the last item of an array, and it does not take arguments. On line 3 I added the ".pop()" function to the array "chores" and removed the "mop floor" element from the initial array. */