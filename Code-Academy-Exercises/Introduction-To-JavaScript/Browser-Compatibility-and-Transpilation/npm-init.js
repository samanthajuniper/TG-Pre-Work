$ ls
$ npm init
$ name — learning-babel
$ description — Use Babel to transpile JavaScript ES6 to ES5
$ ls

#On line 1 I used "ls" to see the file structure of my current directory, which was just "src". On line two I used the "npm init" command to create a package.json file. I then set the name and description properties. Finally, I typed "ls" to view the file structure of my current directory, and found that "package.json" was added to the same level as the src directory.

#Developers use npm to access and manage Node packages. Node packages are directories that contain JavaScript code written by other developers. You can use these packages to reduce duplication and avoid bugs. 