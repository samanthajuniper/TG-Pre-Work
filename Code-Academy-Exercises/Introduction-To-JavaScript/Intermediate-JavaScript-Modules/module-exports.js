const Airplane = {};
Airplane.myAirplane = "StarJet";

module.exports = Airplane;


/*To export modules, the following process should be followed: first define an object to represent the module; then add data or logic to the module; finally export the module. On line 1 I defined a constant varibale "Airplane {};" to represent a module. Then I added the property "myAirplane" with a value of "StarJet" to the module (see line 2). On line 4 I exported the "Airplane" object as a module using the "module.exports =" statement. */