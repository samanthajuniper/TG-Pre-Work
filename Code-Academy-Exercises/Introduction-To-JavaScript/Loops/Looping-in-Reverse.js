// The loop below loops from 0 to 3. Edit it to loop backwards from 3 to 0
for (let counter = 3; counter >= 0; counter--){
  console.log(counter)
};




/* I modified my for loop to run backwards by setting the iterator variable to the highest number in my loop, setting the stopping codition greater than or equal to the lowest number, and telling the loop to subtract one from each iteration. Thus, the console printed "3,2,1,0" (see line 3). */