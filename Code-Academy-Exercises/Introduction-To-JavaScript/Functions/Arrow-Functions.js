const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};



/*The "fat arrow" (=>) notation is a special syntax that can be used to write functions in a shorter way. On line 1 the function expression's syntax is as follows: the parameter "day"  is put first followed by the fat arrow (=>). */